# ============================================================================
# COMPANY CONFIGURATION (Backend)
# ============================================================================
# Configure your organization's branding and contact details
# These values will appear in the UI, generated documents, and email communications
# If not set, defaults to "TechCorp Solutions" demo company

# Company name (appears everywhere in the app)
COMPANY_NAME=TechCorp Solutions

# Application name (used in emails and page titles)
APP_NAME=TechCorp HR Assistant

# ============================================================================
# FRONTEND CONFIGURATION (Next.js)
# ============================================================================
# These must be prefixed with NEXT_PUBLIC_ to be accessible in the browser
# They are embedded at build time

# Company name for frontend display
NEXT_PUBLIC_COMPANY_NAME=TechCorp Solutions

# Application name for frontend display
NEXT_PUBLIC_APP_NAME=TechCorp HR Assistant

# API URL (for connecting frontend to backend)
NEXT_PUBLIC_API_URL=http://localhost:8000

# Company subtitle/tagline (appears on documents)
COMPANY_SUBTITLE=Enterprise Software & HR Solutions Provider

# Company contact details (appears on generated documents)
COMPANY_ADDRESS=123 Business Park, Tech District, Mumbai - 400001
COMPANY_PHONE=+91-22-1234-5678
COMPANY_EMAIL=hr@techcorp.com
COMPANY_WEBSITE=www.techcorp.com

# Optional: Company registration details (leave empty if not applicable)
COMPANY_CIN=
COMPANY_GST=

# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================
# MongoDB Atlas connection string
# Format: mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>
# If not set, the app will fallback to local JSON files
MONGODB_URI=

# ============================================================================
# AI & EXTERNAL SERVICES
# ============================================================================
# Google Gemini API Key (required for AI features)
# Get your API key from: https://makersuite.google.com/app/apikey
GOOGLE_GEMINI_API_KEY=your_gemini_api_key_here

# ============================================================================
# AUTHENTICATION & SECURITY
# ============================================================================
# JWT secret key for token signing (CHANGE THIS IN PRODUCTION!)
# Generate a secure random string: openssl rand -hex 32
JWT_SECRET=your_secret_key_change_in_production

# Email configuration for OTP authentication
# Gmail SMTP settings (you'll need an App Password, not your regular password)
# Instructions: https://support.google.com/accounts/answer/185833
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_gmail_app_password

# Disable authentication (DEVELOPMENT ONLY - DO NOT USE IN PRODUCTION)
DISABLE_AUTH=false

# ============================================================================
# BACKEND CONFIGURATION
# ============================================================================
# Backend API URL (used by frontend to connect to backend)
BACKEND_URL=http://localhost:8000

# ============================================================================
# SECURITY NOTES
# ============================================================================
# 
# IMPORTANT SECURITY CHANGES:
# - Document generation is now restricted to authenticated users only
# - Users can ONLY generate documents for themselves (same-user restriction)
# - Any attempt to generate documents for other users will result in 403 Forbidden
# - There is NO admin bypass - all users are subject to the same restrictions
# 
# This ensures that employees can only access their own personal documents
# and prevents unauthorized access to other employees' sensitive information.
#

